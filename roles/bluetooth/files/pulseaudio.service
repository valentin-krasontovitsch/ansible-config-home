[Unit]
Description=PulseAudio Server
After=avahi-daemon.service network.target
Before=bluetooth.target sound.target

[Service]
ExecStart=/usr/bin/pulseaudio --start -v --daemonize=false \
  --exit-idle-time=-1
Restart=on-failure
User=root
RestartSec=20

[Install]
WantedBy=multi-user.target
